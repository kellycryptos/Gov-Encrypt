{
  "name": "gov-encrypt-monorepo",
  "private": true,
  "workspaces": [
    "app",
    "programs/*",
    "integration"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint",
    "validator": "docker-compose -f docker/docker-compose.yml up -d"
  }
}
